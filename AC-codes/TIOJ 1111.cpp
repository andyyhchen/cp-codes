#include <iostream>
#include <vector>
#define N 501

using namespace std;

char map[N][N];
bool visit[N][N]
vector <int> s;
void dfs(int x, int y, int depth, char now)
{

}

int main()
{
	int n;
	while(~scanf(" %d", &n))
	{
		s.clear();
		for(int i = 0; i < n; i++)
			for(int j = 0; j < n; j++)
			{
				scanf(" %c", &map[i][j]);
				visit[i][j] = false;
			}
			
	}
	return 0;
}
